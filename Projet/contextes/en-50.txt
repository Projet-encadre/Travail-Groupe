in a binary tree). The root node at the top (with
one parent,^[1]^[2] except for the root node, which has no parent
has no parent (i.e., the root node as the top-most node
can be treated like the root node of its own subtree,
one parent, except the topmost root node, which has none. A
node. The height of the root is the height of the
of the path to its root (i.e., its root path). Thus
to its root (i.e., its root path). Thus the root node
its root path). Thus the root node has depth zero, leaf
single node (hence both a root and leaf) has depth and
can be treated as the root node of its own subtree,
path between it and the root node.^[4] This is the same
cycle A→A. A is the root but it also has a
a tree * Finding the root for any node * Finding
level by level, where the root node is visited first, followed
forest) and node (tree with root node with given value and
path),^[6] + with a distinguished root (one vertex is designated as
all descendants. For example, the root node by itself is a
